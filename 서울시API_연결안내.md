# 서울시 도시데이터 통합 연결

사용자의 개인 키는 저장하거나 사용하지 않았습니다. 서울시에서 누구나 조회하도록 제공하는 sample 키로 광화문·덕수궁 응답을 받아 파싱 및 화면을 검증했습니다. ‘공개 샘플로 보기’는 이 응답을 저장해 보여주며 실시간 조회가 아닙니다.

## 연결된 화면

| 데이터 | UI |
|---|---|
| 지역 혼잡 단계·메시지·예측 | 지금 이 지역은? 카드, 시간대별 혼잡 전망 |
| 기온·습도·바람·강수 | 기존 날씨 카드에 서울시 데이터 적용 |
| 미세먼지·초미세먼지·자외선·일몰 | 지역 카드의 환경 정보 |
| 시간대별 기온·하늘 상태·강수확률 | 시간대별 날씨 펼치기 |

추가 연동 범위는 날씨·환경·예보입니다. 문화행사·따릉이·지하철·도로소통 영역과 데이터 가공 코드는 제거했습니다. 상위 API가 통합 응답을 보내더라도 서버는 필요한 정보만 앱에 전달합니다. 기존 혼잡도 기능과 일정 클릭 시 지도·길안내 UI는 유지했습니다. 카카오맵 실제 연동은 별도 작업입니다.

## Render에 올리는 순서

1. 이 폴더의 서버 파일을 GitHub 저장소에 올립니다. `.env`는 올리지 않습니다.
2. Render에서 New → Web Service → 해당 GitHub 저장소를 연결합니다.
3. 이 폴더 자체를 저장소 루트에 올렸다면 Root Directory는 비웁니다. 하위 폴더에 올렸다면 그 경로를 지정합니다.
4. Language: Node, Build Command: `npm install`, Start Command: `npm start`, Health Check Path: `/health`.
5. Environment에 `SEOUL_API_KEY` = 서울시 키, `HOST` = `0.0.0.0`, `ALLOWED_ORIGIN` = GitHub Pages 출처를 설정합니다. 예: `https://your-account.github.io` (저장소 경로와 마지막 / 제외). PORT는 Render가 제공합니다.
6. 서버를 배포하고 `/health`에서 `{"ok":true}`를 확인합니다.
7. `/api/city?area=연남동`에서 `mode: live`와 실제 데이터가 오는지 확인합니다. `unconfigured`는 키 미설정, 502는 인증·상위 API·네트워크 오류입니다.
8. HTML의 `window.SEOULMATE_API_BASE = "";`에 배포한 서버 주소를 넣고 GitHub Pages의 index.html을 교체합니다. 이 주소는 공개 URL이며 키가 아닙니다.

같은 Node 서버의 `/`에서도 앱 전체를 제공합니다. 그 주소로 앱을 열면 API_BASE를 비워도 되고 별도의 CORS 설정이 필요 없습니다.

## 기상청과의 관계

서울시 API 응답에 날씨가 있으므로 서울시 키만 설정해도 조회 지역의 날씨를 사용할 수 있습니다. 서울시 데이터가 표시되는 동안 날씨 새로고침은 해당 표시를 유지하며, 지역 카드의 새로고침이 도시데이터를 재조회합니다. 기상청 경로 `/api/weather`도 유지했습니다. 도시데이터 표시를 해제하면 기존 기상청 설정 또는 예시 모드로 돌아갑니다.

## 지역·혼잡도 제한

- 공식 지정 지역에 대한 정보입니다. 개별 가게 대기 줄, 점포 인원, 예약 현황이 아닙니다.
- 현재 앱에서 조회할 수 있는 지역은 연남동, 홍대 관광특구, 광화문·덕수궁, 서울숲공원, 혜화역, 압구정로데오거리, 합정역입니다. 공식 장소코드로 호출합니다. 지역은 드롭다운에서 확인·변경할 수 있습니다.
- 기존 고정 혼잡도 % 표시는 제거했습니다. 예시 일정 HD_1은 연남동, HD_2는 홍대 관광특구를 참고 지역으로 연결했습니다. 다른 가게의 범위는 아직 검증되지 않아 ‘매장 혼잡도 미제공’으로 표시합니다.
- 다음 일정 경고는 해당 참고 지역을 먼저 조회한 경우에만 작동합니다. 15분 이내 인구 자료, 원천 대체 아님, 붐빔(또는 조용한 취향에서 약간 붐빔)일 때 안내합니다. 모든 계획 지역을 자동으로 조회하거나 매장 혼잡도를 추정하지 않습니다.
- 기존 대체 일정 버튼은 여전히 지정된 예시 일정을 선택합니다. 새 대체 장소가 실제로 덜 붐빈다는 보장은 하지 않습니다.
- 서버는 5분 캐시·동일 지역 중복 요청 병합·15초 제한 시간을 사용합니다. 인구, 날씨, 행사 등의 원천 갱신 시각은 서로 다를 수 있습니다.
- 대기질 ‘점검중’ 등은 수치가 0으로 내려와도 유효한 농도로 표시하지 않습니다. 관측 누락은 정보 없음으로 표시합니다.

## 근거

- [서울시 실시간 도시데이터](https://data.seoul.go.kr/dataList/OA-21285/A/1/datasetView.do)
- [서울시 데이터 안내](https://data.seoul.go.kr/dataVisual/seoul/guide.do)
- [장소코드 및 변경 안내](https://data.seoul.go.kr/together/notice/datasetNoticeView.do?bbsCd=10008&ditcCd=&pageIndex=1&seq=27399e1302ead42db866fa14824c4c56)

현재 검증: 공개 샘플의 실제 응답 정규화, 모의 인증 오류·누락·지역 불일치·캐시·중복 호출, 기상청 회귀 검사. 사용자 키의 승인 범위, Render에서의 상위 API 접근, 전체 지역의 실시간 응답은 키 설정 후 확인해야 합니다.
